package cool.circuit.decorativeNPCS.menusystem.menus;

import cool.circuit.decorativeNPCS.menusystem.SimpleMenu;
import org.bukkit.inventory.ItemStack;

import java.util.List;

public class CreateNPCMenu extends SimpleMenu {
    public CreateNPCMenu(String title, int slots) {
        super(title, slots);
    }
}
